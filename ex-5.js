// Exercise #5: Concatenate the Alphabets
let companyName = "TechUp";

// Start coding here
let count = function(n) {
    for (let i = 0; i < 7; i++) {
        return i;
    }
}

for (let placeName of companyName) {
    console.log("Number " +
    `${n}` + 
    "character " + 
    "is " +
    placeName 
    );
};
